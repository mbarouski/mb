name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build
      run: |
        ./prepare-for-publish.sh
        git add .
        git commit -m "Build [ci skip]"
      
